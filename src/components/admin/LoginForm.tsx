"use client";
import { FormEvent, useState } from "react"; import { useRouter } from "next/navigation";
export function LoginForm(){const router=useRouter();const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setLoading(true);setError("");const data=new FormData(event.currentTarget);try{const response=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:data.get("username"),password:data.get("password")})});const result=await response.json() as {error?:string};if(!response.ok) throw new Error(result.error||"No fue posible iniciar sesión.");router.push("/admin/dashboard");router.refresh()}catch(reason){setError(reason instanceof Error?reason.message:"Error inesperado.")}finally{setLoading(false)}}return <form onSubmit={submit} className="mt-6 min-w-0 space-y-4 sm:mt-8 sm:space-y-5"><label className="block min-w-0"><span className="text-xs font-bold uppercase tracking-widest text-zinc-500">Usuario</span><input name="username" required autoComplete="username" className="mt-2 block min-w-0 w-full border border-zinc-800 bg-black/60 px-3 py-3 outline-none focus:border-red-700 sm:px-4"/></label><label className="block min-w-0"><span className="text-xs font-bold uppercase tracking-widest text-zinc-500">Contraseña</span><input name="password" type="password" required autoComplete="current-password" className="mt-2 block min-w-0 w-full border border-zinc-800 bg-black/60 px-3 py-3 outline-none focus:border-red-700 sm:px-4"/></label>{error&&<p role="alert" className="break-words border-l-2 border-red-600 bg-red-950/30 p-3 text-sm text-red-300">{error}</p>}<button disabled={loading} className="w-full bg-red-700 px-3 py-3 text-xs font-black uppercase tracking-[.12em] transition hover:bg-red-600 disabled:opacity-50 min-[360px]:px-4 min-[360px]:text-sm min-[360px]:tracking-[.18em]">{loading?"Verificando…":"Ingresar"}</button></form>}
