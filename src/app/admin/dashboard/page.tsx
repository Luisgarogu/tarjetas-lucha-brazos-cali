import { redirect } from "next/navigation"; import { ExcelUploadForm } from "@/components/admin/ExcelUploadForm"; import { LogoutButton } from "@/components/admin/LogoutButton"; import { hasValidAdminSession } from "@/lib/auth/admin-session";
export const dynamic="force-dynamic"; export default function Dashboard(){if(!hasValidAdminSession())redirect("/admin/login");return <main className="min-h-screen overflow-x-hidden px-4 py-5 sm:px-6 sm:py-8"><nav className="mx-auto flex max-w-4xl flex-col items-start gap-4 border-b border-zinc-800 pb-5 min-[380px]:flex-row min-[380px]:items-center min-[380px]:justify-between"><div className="text-xs font-black uppercase tracking-[.14em] sm:text-sm sm:tracking-[.2em]">Armwrestling <span className="text-red-600">Cali</span></div><LogoutButton/></nav><section className="mx-auto max-w-4xl py-8 sm:py-12"><p className="text-[10px] font-bold uppercase tracking-[.25em] text-zinc-600 sm:text-xs sm:tracking-[.35em]">Panel de control</p><h1 className="mt-2 break-words text-3xl font-black uppercase italic leading-tight sm:text-4xl">Gestión de credenciales</h1><div className="mt-6 sm:mt-9"><ExcelUploadForm/></div><aside className="mt-6 border-l-2 border-zinc-800 pl-4 text-xs leading-5 text-zinc-500 sm:mt-8 sm:pl-5 sm:text-sm sm:leading-6"><strong className="text-zinc-300">Importante:</strong> las categorías se calculan a partir del peso; no incluya una columna de categoría. En producción, la actualización requiere la integración de GitHub configurada.</aside></section></main>}
