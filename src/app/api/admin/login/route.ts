import { NextResponse } from "next/server"; import { z } from "zod"; import { createAdminSession, safeCredentialMatch } from "@/lib/auth/admin-session";
const schema=z.object({username:z.string().min(1),password:z.string().min(1)}); export async function POST(request:Request){try{const parsed=schema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:"Usuario y contraseña son obligatorios."},{status:400});const user=process.env.ADMIN_USER,password=process.env.ADMIN_PASSWORD;if(!user||!password||!process.env.ADMIN_SESSION_SECRET)return NextResponse.json({error:"La autenticación no está configurada en el servidor."},{status:503});if(!safeCredentialMatch(parsed.data.username,user)||!safeCredentialMatch(parsed.data.password,password))return NextResponse.json({error:"Usuario o contraseña incorrectos."},{status:401});createAdminSession();return NextResponse.json({ok:true})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"No fue posible iniciar sesión."},{status:500})}}
